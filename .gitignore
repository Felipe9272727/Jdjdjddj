.skills/
node_modules/
__pycache__/
*.py[cod]

# cópias runtime do jubileu/public/ na raiz (só os áudios são versionados)
/models/
/textures/

# Modelo de teste da bancada de navegador: 386 MB, baixado sob demanda.
jubileu/bancada-navegador/*.gguf
# O Bergamot: 40 MB de modelo + runtime, buscados por `bergamot/buscar.sh`
jubileu/bancada-navegador/bergamot/

# O wllama do CDN, vendorizado só para a bancada medir o runtime REAL.
# 7,6 MB que o npm/CDN já serve — não entram no repositório.
jubileu/bancada-navegador/wllama-cdn/

# Relatórios dos agentes de revisão: artefato de trabalho, não do jogo.
# Um deles entrou num commit por acidente (um `git add -A` rodado de dentro
# de jubileu/ varre a raiz do repo também), e relatório de varredura envelhece
# mal — vira um retrato de bugs já consertados que alguém lê como se fosse o
# estado atual.
achados-*.md
critica-*.md
